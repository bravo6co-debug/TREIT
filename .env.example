# ⚠️  중요 보안 공지 ⚠️
# 이 파일은 환경변수 설정 예제입니다.
# 실제 사용 시에는 적절한 환경별 파일을 사용하세요:
# - 개발: .env.development (또는 .env.local)
# - 프로덕션: .env.production

# ===========================================
# 클라이언트 환경변수 (VITE_ 접두사)
# 브라우저에서 접근 가능한 공개 정보만!
# ===========================================

# Supabase 공개 설정 (클라이언트에서 사용)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️ 중요: ANON KEY만 클라이언트에서 사용!
# SERVICE ROLE KEY는 절대 클라이언트에 노출하지 마세요!

# ===========================================
# 서버 환경변수 (VITE_ 접두사 없음)
# 서버 사이드에서만 사용되는 민감한 정보!
# ===========================================

# Supabase Service Role Key (⚠️ 매우 중요 - 서버에서만 사용)
# Edge Functions에서만 사용하고, 클라이언트에 절대 노출 금지!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Configuration
SUPABASE_DB_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres
SUPABASE_DB_PASSWORD=your-database-password

# Supabase Local Development (when running locally)
SUPABASE_LOCAL_URL=http://localhost:54321
SUPABASE_LOCAL_ANON_KEY=your-local-anon-key
SUPABASE_LOCAL_SERVICE_ROLE_KEY=your-local-service-role-key

# Auth Configuration
SUPABASE_AUTH_REDIRECT_URL=http://localhost:3000/auth/callback
SUPABASE_AUTH_SITE_URL=http://localhost:3000

# Storage Configuration
SUPABASE_STORAGE_URL=https://your-project.supabase.co/storage/v1

# Edge Functions Configuration
SUPABASE_FUNCTIONS_URL=https://your-project.supabase.co/functions/v1

# Project Configuration
SUPABASE_PROJECT_REF=your-project-ref
SUPABASE_PROJECT_ID=your-project-id

# JWT Configuration
SUPABASE_JWT_SECRET=your-jwt-secret

# Email Configuration (for auth emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Environment
NODE_ENV=development

# App-specific configurations
# User App
VITE_USER_APP_URL=http://localhost:3000
VITE_USER_APP_NAME=TreitMaster User App

# Advertiser Site  
VITE_ADVERTISER_APP_URL=http://localhost:3001
VITE_ADVERTISER_APP_NAME=TreitMaster Advertiser

# Admin Dashboard
VITE_ADMIN_APP_URL=http://localhost:3002
VITE_ADMIN_APP_NAME=TreitMaster Admin

# Payment Configuration (for CPC platform)
PAYMENT_GATEWAY_URL=https://api.payment-provider.com
PAYMENT_GATEWAY_KEY=your-payment-key
PAYMENT_WEBHOOK_SECRET=your-webhook-secret

# Analytics Configuration
ANALYTICS_API_KEY=your-analytics-key

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=100
RATE_LIMIT_REQUESTS_PER_HOUR=1000